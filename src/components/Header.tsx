import {
} from "@mui/material";
import {
    Link,
} from "react-router-dom";
import ApplicationBar from "./ApplicationBar";

function Header() {
    return (
        <div>
            <ApplicationBar />
        </div>
    );
}

export default Header;